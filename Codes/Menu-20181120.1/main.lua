-----------------------------------------------------------------------------------------
--
-- main.lua
--
-----------------------------------------------------------------------------------------

-- Your code here
display.setStatusBar(display.HiddenStatusBar)

local composer = require ("composer")
composer.gotoScene("splash")

local composer = require ("composer")
composer.gotoScene("settings")

local composer = require ("composer")
composer.gotoScene("buildings")

local composer = require ("composer")
composer.gotoScene("buildings")